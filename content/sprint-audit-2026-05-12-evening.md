# Step3D — вечерний sprint audit 2026-05-12

Статус: внутренняя проверка без внешних отправок и публикаций.

## Проверено

- Git: `main` синхронизирован с `origin/main`, рабочее дерево до аудита было чистым.
- Live site: `https://amailab.github.io/Step3D/` отвечает `200`, `<title>` корректный: «Step3D — единое производство 3D-услуг в Москве».
- Локальные HTML-ссылки: критичных битых относительных ссылок не найдено; единственное срабатывание — `/Step3D/` в `404.html`, это ожидаемый абсолютный путь для GitHub Pages.
- Главная страница содержит 2 формы, Telegram CTA, FormSubmit-маршрут, `_next`, UTM-поля и подсказки по материалам заявки.
- Контентная библиотека по лидам уже покрывает: фото → оценка, реверс-инжиниринг, возражения, follow-up 24/72 часа, микропредложения для ниш.

## Лиды

- Скрипт `scripts/check_step3d_leads.py` найден и запускается.
- Проверка Gmail сейчас заблокирована внешней зависимостью: `google-workspace appears offline / Request timed out`.
- Важно для следующих ручных проверок в рамках «без внешних отправок»: запускать мониторинг только в безопасном режиме `--dry-run`, потому что обычный режим может отправлять автоответы новым лидам.

## Быстрый следующий шаг для Никиты

Когда Google Workspace MCP снова доступен, выполнить безопасную проверку:

```bash
Step3D/scripts/check_step3d_leads.py --dry-run
```

Если появятся новые заявки — сначала посмотреть текст автоответа/сводку, потом решать, отправлять ли ответ клиенту.
