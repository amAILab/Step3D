# Runtime-аудит сайта Step3D

Дата: 2026-05-07

## Техническое состояние

- Локальная рабочая копия чистая перед новой итерацией.
- Последний опубликованный коммит перед аудитом: `26f0bb8 Integrate curated Telegram media proof wall`.
- GitHub Pages отвечает 200.
- HTML парсится, `h1` один.
- Внутренние якоря навигации существуют.
- Локальные assets, указанные в HTML/CSS/gallery datasets, существуют.
- JSON-LD валиден.
- `script.js` проходит `node --check`.
- `git diff --check` проходит.

## Наблюдения по UX/дизайну

- Основная структура сайта стала сильной: hero → кейсы/stories → медиа-доказательства → услуги/процесс → заявка.
- Раздел медиа не перегружает сайт тяжёлыми видео, но даёт доказательства из канала.
- Слабое место после добавления медиа: посетителю нужно быстрее считывать, какую компетенцию доказывает каждая карточка.
- Поэтому в этой итерации добавлен слой proof-навигации и короткие proof-теги на карточках.

## Что улучшено в этой итерации

- Добавлена строка направлений компетенций: сценические объекты, промдизайн, роботы, 3D-сканирование, арт-объекты, образование, макеты, прототипы.
- На каждую media-карточку добавлен короткий proof-тег: что именно доказывает публикация.
- Сохранён лёгкий принцип: карточки остаются ссылками на Telegram, видео не встраиваются напрямую.

## Следующие разумные шаги

1. Добавить ручной выбор 3–5 лучших видео и оформить их как отдельный блок «Видео процесса» без автозагрузки.
2. Уточнить реальные коммерческие контакты/телефон, если нужно публиковать не только email/Telegram.
3. После накопления новых кейсов обновлять curated-витрину, а не расширять её бесконечно.

## Продолжение после аудита

- Добавлен лёгкий блок «Видео процесса» внутри медиа-раздела.
- Видео не встраиваются напрямую: карточки ведут в Telegram-посты, чтобы сохранить скорость сайта.
- Выбраны 4 видео-точки входа: 3D-сканирование, награда за 10 дней, аддитивные кроссы, промышленный дизайн.

## Продолжение: выбор формата работы

- Добавлен блок «Как выбрать формат» перед популярными запросами.
- Цель: снизить когнитивную нагрузку для клиента, который не знает, какую услугу выбрать.
- 4 сценария ведут в форму заявки и автоматически подставляют тип проекта/черновик задачи.

## Продолжение: SEO-каталог услуг

- Проверено: FAQPage JSON-LD уже был встроен и совпадает с видимым FAQ.
- Добавлен `hasOfferCatalog` в Organization JSON-LD: 3D-печать, моделирование/промдизайн, реверсивный инжиниринг/сканирование, прототипирование/малая серия, сценические/событийные объекты, образовательные интенсивы.
- Визуально сайт не утяжелён: изменение работает как SEO-разметка.

## Продолжение: UX/UI — 7 улучшений

1. Добавлен верхний индикатор прогресса прокрутки.
2. Навигация в шапке теперь подсвечивает активный раздел.
3. В hero добавлены короткие reassurance-карточки: оценка по фото/эскизу, итерации прототипа, старт без сложных файлов.
4. Перед кейсами добавлена подсказка, что карточки открываются как story-галереи.
5. Фильтр компетенций в медиа-разделе стал интерактивным и скрывает нерелевантные карточки.
6. В форме заявки добавлена живая подсказка по полноте описания задачи.
7. Первый FAQ раскрыт по умолчанию, а открытый вопрос визуально выделяется.

## Продолжение: конверсия и понятность заявки

1. Добавлен раздел `#timeline` с ориентировочными сроками: экспресс-оценка, простой прототип, модель + печать, объект/малая серия.
2. В основную навигацию добавлен пункт «Сроки».
3. В форму заявки добавлена карточка «Готовность заявки» с процентом заполнения.
4. Готовность считает выбранный тип проекта, подробность описания, срок, количество и исходники/файлы.
5. Карточка визуально показывает, какие данные уже есть, а что лучше добавить для быстрой оценки.

Проверка: HTML anchors/assets/JSON-LD, 1 h1, 7 story triggers, 12 media cards, 10 preset buttons, 9 proof filters, timeline, readiness widget, `node --check script.js`, `git diff --check`, локальная отдача.

## Публикационная правка: кейсы, медиа и текст

1. Кейс «Скульптура “Дева Мария”» заменён на более широкий и коммерчески понятный блок «Скульптуры, макеты и мастер-модели».
2. В story этого кейса добавлены материалы по скульптурным объектам, мастер-моделям и архитектурным макетам: 7 кадров вместо 5.
3. Раздел «Медиа из канала» переделан из сетки карточек в одну большую story-подборку.
4. В медиа-story оставлен только контент, который не дублирует основные кейсы: 3D-сканирование, лаборатория, экспериментальные прототипы, промдизайн.
5. Текст сайта вычищен от лишних «нейросетевых» формулировок: видимый бренд приведён к Step3D, AI-формулировки заменены на «цифровые инструменты», убрана узкая привязка к одному объекту «Дева Мария».

Проверка: HTML anchors/assets/JSON-LD, 1 h1, 8 story triggers, 0 media-card дублей, 10 preset buttons, timeline/readiness, `node --check script.js`, `git diff --check`, локальная отдача.

## Финальная правка: мобильное меню, лаконичность и SVG-знак

1. В мобильной версии добавлено burger-меню в стиле Ollama: компактная кнопка в шапке, раскрывающая аккуратную карточку навигации.
2. Верхняя навигация сокращена до ключевых пунктов: кейсы, медиа, услуги, сроки, заявка, контакты.
3. Убраны повторяющиеся блоки `ticker`, `trust-strip`, `stats` и `formats`, чтобы сайт стал короче и спокойнее.
4. Hero-текст упрощён: меньше рекламной воды, больше прямого смысла.
5. SVG-логотип заменён на новый технический анимированный знак: точная геометрия, прозрачный фон, скан-линия, орбита, мигание и поддержка `prefers-reduced-motion`.
6. Удалён устаревший JS для фильтров медиа, которых больше нет после перехода на одну story.

Проверка: HTML anchors/assets/JSON-LD, 1 h1, 8 story triggers, 10 preset buttons, burger menu, SVG parse, `node --check script.js`, `git diff --check`, локальная отдача.
