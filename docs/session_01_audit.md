# Session 01 — полный аудит Step3D

Дата: 2026-05-17

## Проверки

- `python3 scripts/project_healthcheck.py` → OK
- `python3 scripts/validate_site.py` → OK, 32 HTML files
- `python3 scripts/check_analytics_events.py` → OK
- `python3 scripts/check_request_flow.py` → OK
- `python3 scripts/check_backend_templates.py` → OK

## Состояние страниц

| Страница | H1 | Sections | Buttons | Forms | minimal-ui |
|---|---|---:|---:|---:|---|
| `/` | Оценим 3D-задачу по фото или STL | 35 | 80 | 2 | yes |
| `/app/` | Оценить деталь без лишней переписки. | 3 | 10 | 1 | yes |
| `/thanks/` | Заявка создана | 4 | 8 | 0 | yes |
| `/account/` | Статус проекта Step3D | 8 | 12 | 1 | yes |
| `/viewer/` | Просмотр модели и быстрый расчёт печати | 6 | 15 | 1 | yes |

## Главный вывод

Продуктовый путь уже собран: главная → app → заявка → номер S3D → файлы → статус. Но минимализм применён неравномерно:

- `/app/` уже близок к цели.
- Главная всё ещё слишком большая: 35 секций и 80 кнопок. Это нормально для SEO/лендинга, но не для минимального продуктового входа.
- `/account/` визуально упрощён, но в HTML остаются скрытые/демо-блоки, которые лучше вынести ниже или в demo.
- `/viewer/` функционален, но ещё выглядит как отдельный инструмент, а не часть одного простого пути.

## P0

Нет критических поломок. Валидация, Request Flow и deploy-контур зелёные.

## P1

1. Главная перегружена CTA и секциями. Нужен отдельный минимальный top-path: hero → App → 3 шага → trust → форма/FAQ, всё остальное ниже как SEO.
2. Account должен показывать только один активный проект/статус по умолчанию, без ощущения демо-реестра.
3. Viewer должен сильнее завершаться CTA в App: “сохранить расчёт → создать проект”.
4. Нужно добавить проверку minimal UI assets в валидатор, чтобы `minimal-ui.css` не отвалился.

## P2

1. Постепенно вынести inline CSS/JS из `index.html`.
2. Уменьшить число CTA на главной в верхней части.
3. Сделать скриншотный визуальный регресс после крупных UI-правок.

## Следующая сессия

Session 02: довести `/app/` до финального минимального первого экрана и убрать оставшиеся лишние элементы/кнопки, чтобы он стал эталоном для остальных страниц.
